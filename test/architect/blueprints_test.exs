defmodule Architect.BlueprintsTest do
  use Architect.DataCase

  import Architect.Blueprints

  describe "evaluate/2" do
    test "validates the blueprint"
    test "returns errors when the blueprint is invalid"
  end
end
