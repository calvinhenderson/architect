{
  "$id": "schemas/blueprint/task",
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Blueprint task",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[A-z0-9._-]+$",
      "description": "The unique id of the task"
    },
    "type": {
      "type": "string",
      "pattern": "^[A-z0-9._-]+$",
      "description": "The type of plugin that the step will call"
    },
    "description": {
      "type": "string",
      "description": "A string containing a description of the step"
    },
    "inputs": {
      "type": "object",
      "description": "A dictionary of input variables to pass into the step"
    },
    "if": {
      "type": "object",
      "description": "A boolean expression that decides whether a task should run"
    }
  },
  "required": [
    "id",
    "type"
  ]
}
